import Vue from 'vue/dist/vue.esm'
import Router from 'vue-router'

Vue.use(Router)

export default new Router({
  mode: 'history',
  routes: [
/*    {
      path: '/',
      name: 'App',
      component: App
    }
*/
  ]
})
